{
  "name": "@sports/mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "sync": "npm run build && cap sync",
    "ios": "npm run sync && cap open ios",
    "android": "npm run sync && cap open android",
    "test": "vitest run",
    "check": "vue-tsc --noEmit && npm test && npm run build"
  },
  "dependencies": {
    "@sports/core": "0.1.0",
    "@sports/ui": "0.1.0",
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2",
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.0.0",
    "vue": "^3.5.42"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2",
    "@vitejs/plugin-vue": "^6.0.8",
    "vite": "^8.3.0"
  }
}
